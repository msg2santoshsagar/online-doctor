import { ChatTimeIndicatorPipe } from './chat-time-indicator.pipe';

describe('ChatTimeIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatTimeIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
