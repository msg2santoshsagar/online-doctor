import { NewlineHtmlPipe } from './newline-html.pipe';

describe('NewlineHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
