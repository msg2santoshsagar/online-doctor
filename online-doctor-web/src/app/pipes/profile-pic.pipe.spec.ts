import { ProfilePicPipe } from './profile-pic.pipe';

describe('ProfilePicPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilePicPipe();
    expect(pipe).toBeTruthy();
  });
});
